#### chrome-extension-vue-kickstart

#Vue #TypeScript

#### Install
- `npm install && bower install`
- `gulp build`
- Open [chrome://extensions](chrome://extensions) in chrome browser
- Enable `Developer Mode` and click `LOAD UNPACKED`
- Choose `./dist`